package digigun.formats;

/**
 * Minimal build target used to verify that the library compiles successfully.
 */
class BuildTarget {
  /**
   * No-op entry point used by `build.hxml`.
   */
  public static function main():Void {}
}
